[profile.default]
src = "contracts"
test = "test/foundry"
out = "foundry-out"
libs = ["node_modules", "lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
fuzz = { runs = 1000 }
invariant = { runs = 256 }
